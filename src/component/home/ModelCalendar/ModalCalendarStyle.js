import { StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    messageStyle: {
        width: "100%",
        color: "white",
        textAlign: "center"
      }
})
export default styles;